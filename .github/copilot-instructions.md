# ViroConstrictor Copilot Instructions

The canonical project guidance lives in `.agents/AGENTS.md`:

@../.agents/AGENTS.md

Reusable skills live in `.agents/skills/`. This file is only the Copilot discovery adapter. If `@` imports are unavailable, read `../.agents/AGENTS.md` directly.
